const elements = `6434`;

const replaceElements = (getElement) => {
  const arrayElements = [];
  for(let i=0; i<getElement.length; i++){
    arrayElements.push(getElement[i])
  }

  return arrayElements;
}

const searchElements = () => {
  const justItems = replaceElements(elements);
  const testItems = [];



}

searchElements();
